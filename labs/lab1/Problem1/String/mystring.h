#ifndef MYSTRING_H
#define MYSTRING_H

/* Converts a upper case character to lower case */
int to_lower(int a);

/* Convert lower case string to upper case */
void upper_string(char s[]);

/* Check string is alphabet or not */
void check_alphabet(char s[]);

/* Calculate Length of String without Using strlen() Function */
int string_len(char s[]);

#endif